[build]
  publish = "."
  functions = "netlify/functions"

# Make /api/products a clean alias for the function URL
[[redirects]]
  from = "/api/products"
  to = "/.netlify/functions/products"
  status = 200
